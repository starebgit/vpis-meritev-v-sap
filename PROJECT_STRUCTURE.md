# Repository structure

The repository is separated into two root folders:

- `DelphiProject/` - original Delphi codebase and project artifacts.
- `DotNetProject/` - .NET Framework 4.7 WinForms migration workspace.

For SAP trial work, use only files under `DotNetProject/`.
